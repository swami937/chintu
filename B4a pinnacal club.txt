#include <iostream>
#include <string>
using namespace std;


struct Node {
    string name;
    long long prn;
    Node* next;
};


Node* head = NULL;


void addPresident(string name, long long prn) {
    Node* newNode = new Node();
    newNode->name = name;
    newNode->prn = prn;
    newNode->next = head;
    head = newNode;
    cout << "President added successfully.\n";
}


void addSecretary(string name, long long prn) {
    Node* newNode = new Node();
    newNode->name = name;
    newNode->prn = prn;
    newNode->next = NULL;

    if (head == NULL) {
        head = newNode;
    } else {
        Node* temp = head;
        while (temp->next != NULL)
            temp = temp->next;
        temp->next = newNode;
    }
    cout << "Secretary added successfully.\n";
}


void addMember(string name, long long prn) {
    if (head == NULL) {
        cout << "Add president first!\n";
        return;
    }

    Node* newNode = new Node();
    newNode->name = name;
    newNode->prn = prn;

    Node* temp = head;
    while (temp->next != NULL && temp->next->next != NULL)
        temp = temp->next;

    newNode->next = temp->next;
    temp->next = newNode;
    cout << "Member added successfully.\n";
}


void display() {
    Node* temp = head;
    if (temp == NULL) {
        cout << "No members in the club.\n";
        return;
    }
    cout << "\n--- Club Members ---\n";
    while (temp != NULL) {
        cout << "PRN: " << temp->prn << " | Name: " << temp->name << endl;
        temp = temp->next;
    }
}


void countMembers() {
    Node* temp = head;
    int count = 0;
    while (temp != NULL) {
        count++;
        temp = temp->next;
    }
    cout << "\nTotal Members: " << count << endl;
}


int main() {
    int choice;
    string name;
    long long prn;

    do {
        cout << "\n===== Pinnacle Club Menu =====";
        cout << "\n1. Add President";
        cout << "\n2. Add Member";
        cout << "\n3. Add Secretary";
        cout << "\n4. Display Members";
        cout << "\n5. Count Members";
        cout << "\n6. Exit";
        cout << "\nEnter your choice: ";
        cin >> choice;

        switch (choice) {
            case 1:
                cout << "Enter President Name: ";
                cin >> name;
                cout << "Enter PRN: ";
                cin >> prn;
                addPresident(name, prn);
                break;

            case 2:
                cout << "Enter Member Name: ";
                cin >> name;
                cout << "Enter PRN: ";
                cin >> prn;
                addMember(name, prn);
                break;

            case 3:
                cout << "Enter Secretary Name: ";
                cin >> name;
                cout << "Enter PRN: ";
                cin >> prn;
                addSecretary(name, prn);
                break;

            case 4:
                display();
                break;

            case 5:
                countMembers();
                break;

            case 6:
                cout << "Exiting program.\n";
                break;

            default:
                cout << "Invalid choice.\n";
        }
    } while (choice != 6);

    return 0;

}

⚙️ Main Concept — Linked List

Each member is stored as a Node (name, prn, next pointer).

All nodes are connected in a chain (President → Members → Secretary).

head points to the first node (President).

🧩 Functions Explained (Short & Simple)

addPresident(name, prn)

Creates the first node (President).

Inserts it at the beginning of the list.

addSecretary(name, prn)

Creates the last node (Secretary).

Adds it at the end of the list.

addMember(name, prn)

Adds a member between President and Secretary.

Traverses till second-last node and inserts before the Secretary.

display()

Shows all members (President → Members → Secretary) in order.

countMembers()

Counts and displays the total number of nodes in the list.

🖥️ main() Function

Shows a menu to:

Add President

Add Member

Add Secretary

Display Members

Count Members

Exit

Calls the respective functions based on user choice.

⏱ Time Complexity
Operation	Complexity
Add / Display / Count	O(n)
Search (implicit in traversal)	O(n)
