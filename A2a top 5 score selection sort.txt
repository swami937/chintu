#include <iostream>
using namespace std;

// ---------- Function: Selection Sort ----------
void SelectionSort(float arr[], int n) {
    for (int i = 0; i < n - 1; i++) {
        int minIndex = i;
        for (int j = i + 1; j < n; j++) {
            if (arr[j] < arr[minIndex])
                minIndex = j;
        }
        // Swap
        float temp = arr[i];
        arr[i] = arr[minIndex];
        arr[minIndex] = temp;
    }
    cout << "Array sorted successfully (ascending order)." << endl;
}

// ---------- Function: Display Top 5 Scores ----------
void DisplayTop5(float arr[], int n) {
    cout << "Top 5 Scores: ";
    int count = 0;
    // Array is sorted ascending ? so start from end
    for (int i = n - 1; i >= 0 && count < 5; i--) {
        cout << arr[i] << " ";
        count++;
    }
    cout << endl;
}

// ---------- Function: Display All Percentages ----------
void Display(float arr[], int n) {
    cout << "Student Percentages: ";
    for (int i = 0; i < n; i++)
        cout << arr[i] << " ";
    cout << endl;
}

// ---------- Main Function ----------
int main() {
    int n, ch;
    float perc[50];

    cout << "Enter number of students: ";
    cin >> n;

    cout << "Enter first year percentages:\n";
    for (int i = 0; i < n; i++)
        cin >> perc[i];

    do {
        cout << "\n--- MENU ---\n";
        cout << "1. Sort Percentages (Selection Sort)\n";
        cout << "2. Display All Percentages\n";
        cout << "3. Display Top 5 Scores\n";
        cout << "4. Exit\n";
        cout << "Enter your choice: ";
        cin >> ch;

        switch (ch) {
            case 1:
                SelectionSort(perc, n);
                break;

            case 2:
                Display(perc, n);
                break;

            case 3:
                SelectionSort(perc, n); // Ensure sorted before showing top 5
                DisplayTop5(perc, n);
                break;

            case 4:
                cout << "Exiting program..." << endl;
                break;

            default:
                cout << "Invalid choice, please try again." << endl;
        }
    } while (ch != 4);

    return 0;
}
